/**
 * Hire challenge: CV must be served from /public.
 * Filename must match the PDF placed in the public folder.
 */
export const CV_PUBLIC_FILENAME = 'Tareq_Hasan_FullStack_Developer_CV.pdf.pdf'

/** URL path (Vite serves public files at root). */
export const CV_PATH = `/${CV_PUBLIC_FILENAME}`

/** Suggested name when users hit Download (avoids double “.pdf” in saves). */
export const CV_DOWNLOAD_NAME = 'Tareq_Hasan_FullStack_Developer_CV.pdf'

export const CV_CANDIDATE_NAME = 'Tareq Hasan'
export const CV_CANDIDATE_ROLE = 'Full-stack developer'
